### 1. laa-function
```ash
#This Program and function was compiled with musl in a alpine environment.
#It may wont work on an env with glibc without recompiling inside the environment.

clang laa.c -o laa && ./laa
```
