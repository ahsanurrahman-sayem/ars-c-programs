### These c programs were compiled in a alpine environment.

### 1. laa-function
```ash
# I made this cmd-tool to list hidden dirs inside the current directory.
# this function prints only hidden dirs to the stdout.

clang laa.c -o laa && ./laa
```
### 2. mgcc
```ash
# This cmd-tool combines gcc and make into single command.
# I dont use it anymore.
# Just go inside the 'mgcc' dir and run -

make
```
